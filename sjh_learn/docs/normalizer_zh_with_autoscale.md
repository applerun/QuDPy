# Normalizer 文档已迁移

当前 normalizer 设计说明见：

- [normalizer_zh.md](normalizer_zh.md)

本文件名保留用于旧链接兼容。旧版本中关于从单一 `omega_L_fs_inv` 计算固定
`0 -> 1` detuning candidate 的说明已经过时：当前 `ParaNormalizer` 不再要求 field
提供单一 carrier frequency，也不再把单一 detuning 作为 lab_exact 主路径依赖。
